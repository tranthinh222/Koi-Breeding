info:
  name: Delete User
  type: http
  seq: 3

http:
  method: DELETE
  url: localhost:8090/koi_breeding/admin/users?id=1
  params:
    - name: id
      value: "1"
      type: query
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
