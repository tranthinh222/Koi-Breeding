info:
  name: Update User Status (ban, unban, lock)
  type: http
  seq: 4

http:
  method: PUT
  url: localhost:8090/koi_breeding/admin/users?id=1
  params:
    - name: id
      value: "1"
      type: query
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
