info:
  name: get trade
  type: http
  seq: 4

http:
  method: GET
  url: localhost:8090/koi_breeding/admin/trade?search=demo_user
  params:
    - name: search
      value: demo_user
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
