info:
  name: Forgot Password
  type: http
  seq: 3

http:
  method: POST
  url: localhost:8090/koi_breeding/auth/forgot-password
  body:
    type: json
    data: |-
      {
        "email": "safepoison33891@gmail.com" 
      }
      // Data = null, check mail
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
