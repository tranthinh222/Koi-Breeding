info:
  name: Get dictionaries
  type: http
  seq: 4

http:
  method: GET
  url: "http://localhost:8090/koi_breeding/api/v1/dictionaries?page=0&size=10"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
