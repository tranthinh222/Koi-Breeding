info:
  name: Add inventory item
  type: http
  seq: 2

http:
  method: POST
  url: "http://localhost:8090/koi_breeding/api/v1/inventory/items/:itemId/addition?userId=1"
  params:
    - name: itemId
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {"quantity": 1}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
