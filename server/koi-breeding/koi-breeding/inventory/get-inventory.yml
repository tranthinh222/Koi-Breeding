info:
  name: Get inventory
  type: http
  seq: 1

http:
  method: GET
  url: "http://localhost:8090/koi_breeding/api/v1/inventory?userId=1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
