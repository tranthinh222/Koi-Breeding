info:
  name: Delete koi
  type: http
  seq: 6

http:
  method: DELETE
  url: "http://localhost:8090/koi_breeding/api/v1/kois/:id"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
