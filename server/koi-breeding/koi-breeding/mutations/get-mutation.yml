info:
  name: Get mutation
  type: http
  seq: 3

http:
  method: GET
  url: "http://localhost:8090/koi_breeding/api/v1/mutations/:id"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
