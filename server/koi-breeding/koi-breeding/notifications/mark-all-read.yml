info:
  name: Mark all notifications read
  type: http
  seq: 3

http:
  method: POST
  url: "http://localhost:8090/koi_breeding/api/v1/users/:userId/notifications/read"
  params:
    - name: userId
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
