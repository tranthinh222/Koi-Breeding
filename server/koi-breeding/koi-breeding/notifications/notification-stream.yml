info:
  name: Subscribe notification stream
  type: http
  seq: 2

http:
  method: GET
  url: "http://localhost:8090/koi_breeding/api/v1/users/:userId/notifications/stream"
  params:
    - name: userId
      value: "1"
      type: path
  headers:
    - name: Accept
      value: text/event-stream
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
