info:
  name: get transaction
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8090/koi_breeding/users/2/transactions?page=0&size=5&sort=createdAt,desc
  params:
    - name: page
      value: "0"
      type: query
    - name: size
      value: "5"
      type: query
    - name: sort
      value: createdAt,desc
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
