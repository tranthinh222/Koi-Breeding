info:
  name: Upload avatar
  type: http
  seq: 1

http:
  method: POST
  url: "http://localhost:8090/koi_breeding/api/v1/upload/avatar"
  body:
    type: multipart-form
    data:
      - name: file
        type: file
        value:
          - ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
