info:
  name: Get user
  type: http
  seq: 1

http:
  method: GET
  url: "http://localhost:8090/koi_breeding/api/v1/users/:id"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
