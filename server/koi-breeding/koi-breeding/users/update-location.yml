info:
  name: Update user location
  type: http
  seq: 2

http:
  method: PATCH
  url: "http://localhost:8090/koi_breeding/api/v1/users/:id/location"
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {"location": "HO_CHI_MINH_CITY"}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
