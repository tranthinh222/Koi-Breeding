info:
  name: Get wallet balance
  type: http
  seq: 1

http:
  method: GET
  url: "http://localhost:8090/koi_breeding/api/v1/wallet/:userId"
  params:
    - name: userId
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
